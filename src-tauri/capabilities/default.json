{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the whitebox desktop app",
  "windows": ["main"],
  "permissions": ["core:default"]
}
